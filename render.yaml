services:
  - type: web
    name: divvyup-backend
    env: node
    buildCommand: cd backend && npm ci && npm run build:production
    startCommand: cd backend && npm run prisma:migrate:production && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: divvyup-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 10000
      - key: CORS_ORIGIN
        value: https://divvyup.vercel.app

databases:
  - name: divvyup-db
    databaseName: divvyup
    user: divvyup
